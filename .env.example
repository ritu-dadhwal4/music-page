# Passcode that unlocks settings mode at /?edit=<EDIT_KEY>. Required.
EDIT_KEY=change-me

# Optional. When set, /api/youtube returns real track durations via the
# YouTube Data API. Without it, durations come back null.
YOUTUBE_API_KEY=
